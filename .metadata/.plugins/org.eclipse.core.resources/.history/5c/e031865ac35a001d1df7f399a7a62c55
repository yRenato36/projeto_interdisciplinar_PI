#pagina-principal-usuario {
    margin: 20px;
    min-width: 1020px;
    background-color: var(--color-background-one);
}

#principal {
    max-width: 1440px;
    min-width: 1100px;
    margin: 20px auto;
    padding: 20px;
    background-color: var(--color-background-two);
    border: solid 2px var(--color-botder-container);
    border-radius: 30px;
}

.desenhos {
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.desenhos img {
    height: 200px;
}

.desenhos img:nth-child(2){
    height: 100px;
}

#principal > strong {
    display: block;
    text-align: center;
    
    color: var(--color-text-title);
    font-size: 1.8rem;
    font-weight: 800;
    font-family: var(--font-family-title);   
}

#pesquisa-empresa {
    margin: 40px auto;
    max-width: 800px;
    
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-auto-rows: 40px;

    align-items: flex-end;
    gap: 15px;
}

#pesquisa-empresa label {
    color: var(--color-text-label);
    font-size: 1.2rem;
    font-weight: normal;
    font-family: var(--color-text-label);
}

#pesquisa-empresa select {
    width: 100%;
    height: 40px;
    margin-top: 0.2rem;

    background-color: var(--color-background-input);
    border: solid 1px var(--color-border-buttons);
    border-radius: 10px;

    padding: 0 8px;

    font-size: 1.2rem;
    font-family: var(--font-family-description);

    cursor: pointer;
}

#pesquisa-empresa button {
    width: 100%;
    height: 40px;
    
    background-color: var(--color-background-buttons);
    border: solid 1px var(--color-border-buttons);
    border-radius: 10px;

    font-size: 1.2rem;
    font-weight: 700;
    font-family: var(--font-family-button);

    grid-column: 2/3;
    display: flex;
    align-items: center;
    justify-content: center;

    transition: background 0.3s;
    cursor: pointer;   
}

#pesquisa-empresa button:hover {
    background-color: var(--color-background-three);
}

.resultado-pesquisa {
    max-width: 1050px;
    min-width: 1050px;
    height: 450px;
    margin: 20px auto;

    border: solid 1px var(--color-botder-container);
    border-radius: 20px;
    background-color: var(--color-background-one);

    padding: 0 10px;
    overflow-y:scroll;
}

.resultado-pesquisa .resultado {
    width: 100%;
    height: 60px;
    margin: 10px auto;
    
    background-color: var(--color-background-two);
    border: solid 5px var(--color-border-buttons);
    border-radius: 30px;

    cursor: pointer;
    transition: background 0.3s;

    display: flex;
    align-items: center;
    justify-content: space-between;
}

.resultado-pesquisa .resultado:hover {
    background-color: var(--color-background-three);
}

.resultado-pesquisa .resultado > hr {
    width: 1px;
    height: 80%;

    border: 0;
    background-color: var(--color-border-buttons);
}

.resultado-pesquisa .resultado span {
    margin: auto;

    color: var(--color-text-description);
    font-size: .8rem;
    font-weight: normal;
    font-family: var(--font-family-button);
}

#sobre {
    max-width: 1050px;
    min-width: 1050px;
    margin: auto;
    margin-top: 15px;
    position: relative;
    padding: 10px;
}

#sobre h2 {
    color: var(--color-text-title);
    font-size: 1.8rem;
    font-weight: 700;
    font-family: var(--font-family-title);
    margin-bottom: 5px;
}

#sobre p {
    color: var(--color-text-description);
    font-size: 1rem;
    font-weight: normal;
    font-family: var(--font-family-description);
    text-align: justify;
    line-height: 1.5rem;
}

.linhas div {
    position: absolute;
    width: 100%;
    right: 0;
    left: 0;
    top: 0;  
    opacity: 50%;
    border-bottom: solid 1px var(--color-botder-container);
}

.linhas div:nth-child(1) { 
    margin-top: 40px;
}
.linhas div:nth-child(2) {
    margin-top: 70px;
}

.linhas div:nth-child(3) {
    margin-top: 94px;
}
.linhas div:nth-child(4) {
    margin-top: 118px;
}
.linhas div:nth-child(5) {
    margin-top: 142px;
}
.linhas div:nth-child(6) {
    margin-top: 166px;
}

#contato {
    max-width: 1440px;
    min-width: 1024px;
    margin: 20px auto;
    min-width: 1100px;
    padding: 20px;

    background-color: var(--color-background-two);
    border: solid 2px var(--color-botder-container);
    border-radius: 30px; 

    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-row: 1fr;
    grid-template-areas: "contact support advice"; 
    gap: 15px;
}

.contato-conteudo {
    grid-area: contact;
    display: inline;
    text-align: center;
}
.contato-conteudo strong {
    color: var(--color-text-label);
    font-size: 1.2rem;
    font-weight: 700;
    font-family: var(--font-family-label);
}
.contato-conteudo img{
    margin: 10px 10px 0 10px;
    width: 30px;
}

.suporte-duvidas {
    grid-area: support;
    margin: 0 auto;
}
.suporte-duvidas strong {
    color: var(--color-text-label);
    font-size: 1.2rem;
    font-weight: 700;
    font-family: var(--font-family-label);
    line-height: 40px;
}
.suporte-duvidas span {
    color: var(--color-text-label);
    font-size: 1rem;
    font-weight: normal;
    font-family: var(--font-family-label);
    line-height: 25px;
}

.assessoria {
    margin: 0 auto;
}

.assessoria strong {
    color: var(--color-text-label);
    font-size: 1.2rem;
    font-weight: 700;
    font-family: var(--font-family-label);
    line-height: 40px;
}

.assessoria span {
    color: var(--color-text-label);
    font-size: 1rem;
    font-weight: normal;
    font-family: var(--font-family-label);
    line-height: 20px;
}